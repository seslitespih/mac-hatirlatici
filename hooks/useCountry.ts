// Re-exports from CountryContext so all existing imports keep working.
// The real state lives in CountryProvider (see contexts/CountryContext.tsx).
export { useCountry } from '../contexts/CountryContext';
